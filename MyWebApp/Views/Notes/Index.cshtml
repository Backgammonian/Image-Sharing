@*
    List of available notes
*@

@model NotesListViewModel

@if (Model.NotesDetails.Count() == 0)
{
    <h2>There are no notes yet.</h2>
}
else
{
    var notesDetails = Model.NotesDetails;

    <h1>Notes</h1>  
    foreach (var noteDetail in notesDetails)
    {
        var note = noteDetail.Note;
        if (note == null)
        {
            continue;
        }

        var score = noteDetail.Score;
        var thread = noteDetail.Thread;
        var firstImage = noteDetail.Images.FirstOrDefault();
        var author = noteDetail.Author;
        var authorProfilePicture = noteDetail.ProfilePicture;

        <div>
            <div>
                if (@author != null)
                {
                    <img width="100" height="100" src="@ImagePathHelper.GetPath(@authorProfilePicture.ImageFileName)" asp-append-version="true"/>
                    <a asp-controller="Users" asp-action="Details" asp-route-userId="@author.Id" class="btn btn-sm btn-outline-secondary">Author: @author.UserName</a>
                }
                else
                {
                    <img width="100" height="100" src="@ImagePathHelper.GetDefaultImagePath()" asp-append-version="true"/>
                    <a class="btn btn-sm btn-outline-secondary">Unknown author</a>
                }
            </div>

            <h3>@note.Title</h3>
            <h4>@note.Description</h4>
            @if (firstImage != null)
            {
                <img src="@ImagePathHelper.GetPath(firstImage.ImageFileName)" asp-append-version="true"/>
            }

            <h4>Rating: @score</h4>
            <h4>Thread:</h4>
            <a asp-controller="Threads" asp-action="GetByThread" asp-route-thread="@thread" class="btn btn-sm btn-outline-secondary">@thread</a>

            <div>
                if (User.IsAuthenticated())
                {
                    <a asp-controller="Ratings" asp-action="Upvote" asp-route-noteId="@note.NoteId" class="btn btn-sm btn-outline-secondary">👍</a>
                    <a asp-controller="Ratings" asp-action="Downvote" asp-route-noteId="@note.NoteId" class="btn btn-sm btn-outline-secondary">👎</a>
                }
            </div>

            <div>
                if (User.IsAdmin() ||
                    User.IsOwner(note))
                {
                    <a asp-controller="Notes" asp-action="Edit" asp-route-noteId="@note.NoteId" class="btn btn-sm btn-outline-secondary">Edit</a>
                    <a asp-controller="Notes" asp-action="Delete" asp-route-noteId="@note.NoteId" class="btn btn-sm btn-outline-secondary">Delete</a>
                }

                <a asp-controller="Notes" asp-action="Details" asp-route-id="@note.NoteId" class="btn btn-sm btn-outline-secondary">View</a>
            </div>
        </div>
    }
}